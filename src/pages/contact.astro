---
import { Icon } from "astro-icon";
import Socials from "../components/socials.astro";
import Layout from "../layouts/Layout.astro";
import sgMail from "@sendgrid/mail";

if (Astro.request.method === "POST") {
  try {
    const data = await Astro.request.formData();
    const name = data.get("name");
    const email = data.get("email");
    const phone = data.get("phone");
    const formMessage = data.get("message");

    const message = {
      to: "zachwagner@hey.com",
      from: "zachwagner@hey.com",
      subject: "New message from contact form",
      text: `Name: ${name}\nEmail: ${email}\nPhone: ${phone}\nMessage: ${formMessage}`,
    };
    sgMail.setApiKey(import.meta.env.SENDGRID_API_KEY);
    await sgMail.send(message);
  } catch (error) {
    if (error instanceof Error) {
      console.error(error.message);
    }
  }
}
---

<script>
  import confetti from "canvas-confetti";

  // Find form DOM on the page.
  const form = document.querySelector("[data-confetti-form]") as HTMLFormElement;

  form?.addEventListener("submit", (e) => {
    // Set a flag in localStorage to remember that the form has been submitted.
    localStorage.setItem("formSubmitted", "true");

    // Allow the form to submit normally.
    return true;
  });

  const count = 200;
  const defaults = {
    origin: { y: 0.7 },
  };

  function fire(
    particleRatio: number,
    opts: { spread: number; startVelocity?: number; decay?: number; scalar?: number }
  ) {
    confetti(
      Object.assign({}, defaults, opts, {
        particleCount: Math.floor(count * particleRatio),
      })
    );
  }
  // Check if the form has been submitted.
  const submitted = localStorage.getItem("formSubmitted");

  if (submitted) {
    // Trigger the confetti animation.
    fire(0.25, {
      spread: 26,
      startVelocity: 55,
    });
    fire(0.2, {
      spread: 60,
    });
    fire(0.35, {
      spread: 100,
      decay: 0.91,
      scalar: 0.8,
    });
    fire(0.1, {
      spread: 120,
      startVelocity: 25,
      decay: 0.92,
      scalar: 1.2,
    });
    fire(0.1, {
      spread: 120,
      startVelocity: 45,
    });

    // Remove the submitted flag from localStorage.
    localStorage.removeItem("formSubmitted");
  }
</script>

<Layout title="contact">
  <div class="relative">
    <div class="absolute inset-0">
      <div class="absolute inset-y-0 left-0 w-1/2"></div>
    </div>
    <div class="relative mx-auto h-fit max-w-7xl rounded-xl shadow-xl lg:grid lg:grid-cols-5">
      <div class="py-16 px-4 sm:px-6 lg:col-span-2 lg:px-8 lg:py-24 xl:pr-12">
        <div class="mx-auto max-w-lg">
          <h2 class="text-2xl font-extrabold tracking-tight text-gray-900 sm:text-3xl">
            Get in touch
          </h2>
          <p class="mt-3 text-lg leading-6 text-gray-500">
            Feel free to message me about anything and everything!
          </p>
          <Socials />
        </div>
      </div>
      <div class="py-16 px-4 sm:px-6 lg:col-span-3 lg:py-24 lg:px-8 xl:pl-12">
        <div class="mx-auto max-w-lg lg:max-w-none">
          <form data-confetti-form action="#" method="POST" class="grid grid-cols-1 gap-y-6">
            <div class="flex flex-col gap-2">
              <label for="name" class="">Name*</label>
              <input
                type="text"
                name="name"
                id="name"
                autocomplete="name"
                class="block w-full rounded-md border-2 bg-transparent py-3 px-4 placeholder-gray-500 shadow-sm focus:border-indigo-500 focus:ring-indigo-500"
                placeholder="Your Name"
                required
              />
            </div>
            <div class="flex flex-col gap-2">
              <label for="email" class="">Email*</label>
              <input
                id="email"
                name="email"
                type="email"
                autocomplete="email"
                class="block w-full rounded-md border-2 bg-transparent py-3 px-4 placeholder-gray-500 shadow-sm focus:border-indigo-500 focus:ring-indigo-500"
                placeholder="you@company.com"
                required
              />
            </div>
            <div class="flex flex-col gap-2">
              <label for="phone" class="">Phone (optional)</label>
              <input
                type="text"
                name="phone"
                id="phone"
                autocomplete="tel"
                class="block w-full rounded-md border-2 bg-transparent py-3 px-4 placeholder-gray-500 shadow-sm focus:border-indigo-500 focus:ring-indigo-500"
                placeholder="(111)-222-3333"
              />
            </div>
            <div class="flex flex-col gap-2">
              <label for="message" class="">Message*</label>
              <textarea
                id="message"
                name="message"
                rows="4"
                class="block w-full rounded-md border-2 bg-transparent py-3 px-4 placeholder-gray-500 shadow-sm focus:border-indigo-500 focus:ring-indigo-500"
                placeholder="Your Message"
                required></textarea>
            </div>
            <div>
              <button
                type="submit"
                class="inline-flex justify-center rounded-md border border-transparent bg-indigo-600 py-3 px-6 text-base font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
                >Submit</button
              >
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</Layout>
